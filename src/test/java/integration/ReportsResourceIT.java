package integration;

import com.jakobniinja.controllers.ReportsResourceTest;
import io.quarkus.test.junit.QuarkusIntegrationTest;

@QuarkusIntegrationTest
public class ReportsResourceIT extends ReportsResourceTest {
    // Execute the same tests but in packaged mode.
}
